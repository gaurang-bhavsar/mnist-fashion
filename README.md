# mnist-fashion
PyTorch-based ANN for classifying fashion items (10 categories) using GPU acceleration. 4-layer architecture (784→512→256→128→64→10) achieves 98.8% training and 89.0% test accuracy on Fashion MNIST dataset. Built with CUDA support, Adam optimizer, and custom DataLoader implementation.
